# 1ai-content — Multi-Agent Audit Report (Final)

## 1. Provenance

- **Audit type:** Read-only static analysis (no code changes made, nothing fixed)
- **Method:** 56 subagents (`AgentSwarm`), one per scope area, all completed with outcome `completed`, 0 failures
- **Audit date:** 2026-08-03
- **Source:** AgentSwarm output file `.../wd_1ai-content_8183385057e0/session_201c76ed-115e-4083-af83-bc75077d822e/agents/main/tool-results/AgentSwarm-call_e275e54ac804417ab9659219-59d8627d-a076-48c1-93ad-2ab18f7ffc86.txt` (1525 lines, read in full)
- **Scope:** entire repo — TS bot core (src/), routes, handlers/flows/workers/menus/middleware/config/utils/repositories, i18n/MCP, vendored tools, Python media-api (core+routers+sub-services), admin-ui, tests, prisma, Docker/CI/systemd, scripts, config/.env.example, docs/AGENTS.md

## 2. Severity Tally (approximate — caveat below)

| Scope area | HIGH | MED | LOW |
|---|---|---|---|
| TS bot core (services/config/utils/repos) | ~12 | ~28 | ~40 |
| Commands & TikTok/YT commands | 4 | 6 | 6 |
| Routes TS | 4 | 12 | 14 |
| Handlers/flows/workers/menus/middleware | 15 | 10 | 8 |
| i18n/MCP/views/public | 1 | 4 | 4 |
| Vendored tools (vidbee/krillinai) | 4 | 4 | 3 |
| Python media-api (core+routers+sub-services) | ~26 | ~50 | ~60 |
| admin-ui | 12 | 16 | 12 |
| tests | 3 | 6 | 8 |
| prisma | 6 | 6 | 4 |
| Docker/CI/systemd/nginx | 3 | 4 | 4 |
| scripts | 1 | 4 | 9 |
| config/.env.example | 3 | 6 | 4 |
| docs/AGENTS/README/WORKFLOW/CHANGELOG | 4 | 9 | 8 |
| **Totals (all 56 agents)** | **≈100** | **≈230** | **≈320** |

Counts are per-subagent severity labels, not independently re-verified (±10%). All 56 agents were read; no scope missing.

## 3. Findings by Scope Area

### 3.1 TS bot core — services
- **[HIGH — RCE] Command injection, 2 places:** `src/services/content-pipeline.service.ts:81-82,121-122` interpolates a Telegram-supplied URL into `execSync('python3 -m yt_dlp ... "${url}"')` (only substring regex checks) → arbitrary command execution; same class in `src/services/audio-vo.service.ts:100-104` (edge-tts shell string escapes only `"`).
- **[HIGH] Wrong DB column in raw SQL:** `subscription.service.ts:73,211` `$executeRaw UPDATE users SET subscription_credits ... WHERE telegramId=...` — real column is `telegram_id` (schema `@map`) → PG 42703; grant/expiry throws (cross-confirmed by a55).
- **[HIGH] Dead/misleading providers:** `image-img2img.ts:484` Segmind posts with no auth header (sibling :446 sends it) → always 401; `provider-registry.ts:271` module unimported (dead); `video-async.ts:652,661` builds `${OMNIROUTE_URL}/v1/videos/generations` but env default ends `/v1` → double `/v1`; quota-tracker hardcodes used=0 → quota never blocks.
- **[HIGH] Fabricated data:** `youtube-api.service.ts:139-167` canned analytics zeros persisted to `ytVideoMetrics` (breakout/quarantine/triage dead); 20-file YT pipeline unwired (9 BullMQ queues never `.add()`'d); `fallback.ts:24-41` return `success:true` when all providers fail → outages masked.
- **[HIGH] MCP IDOR:** `src/mcp/server.ts:287-297` `get_ebook_status`/`list_ebooks` call `getStatus(projectId)`/`listProjects` with no ownership check → any MCP caller reads any user's ebook.
- **[HIGH] Email never sent:** `src/utils/email.ts` `ConsoleEmailService` only console.logs verification/reset URLs, yet auth endpoints reply "Check your email"; `/verify-email` & `/reset-password` are GET links to endpoints that don't exist (only POST) → email signup-verification and forgot-password non-functional end-to-end.
- **[HIGH] Notification queue has no consumer:** `config/queue.ts:41,183` — `/api/broadcast` enqueues, returns `success:true`, messages never delivered.
- **[MED]** Billing mischarges: `pricing.ts:265-275` never falls through to custom tiered pricing (videos >60s stuck at flat VIDEO_120S); `dynamic-pricing` providerKey `'omniroute'` not in PROVIDER_CONFIG → costUsd 0; refunds without dedup; `1AI_PAYMENT_URL` default `localhost:3103` vs documented 3100. Omniroute bug `(error as any).response?.data?.(error as Error)?.message` → `data?.()` TypeError (`omniroute.service.ts:254,307,348`). SSRF: `url-validator` guards only static patterns — private-IP hostnames + decimal/hex/octal IP literals bypass; `validateUrlWithDns` never used. Rate limiter (`rateLimit.ts:204-242`) reads `request.user?.telegramId` but nothing assigns `request.user` → per-IP bucket for all users; user middleware writes prisma `lastActivityAt` per message. `money-printer-turbo/` is empty → spawn always fails.

### 3.2 Commands
- **[HIGH] `/broadcast` does not send:** `admin/broadcast.ts:143-158` logs "Would broadcast..." and replies "queued".
- **[HIGH] Dead-end command callbacks:** `calendar.ts:88-96` schedule dead-ends (`cal_type_*` buttons no handler, `scheduleContent()` zero callers); `carousel.ts:76-77` `carousel_publish_*`/`carousel_edit_caption_*` unhandled → "Unknown callback".
- **[MED]** `systemStatus.ts:43-45` hardcoded fake metrics ("Active Users: 1,234", "Error Rate: 0.3%") + unconditional "🟢 Connected"; payment settings gated on SUPER_ADMIN_IDS vs ADMIN_TELEGRAM_IDS; `/yt_*` never imported → unregistered but referenced in help.

### 3.3 Routes TS
- **[HIGH] Ebook IDOR (cross-tenant):** `content-api.ts:234-302` + `ebook.service.ts:143,171,179` — ebook endpoints not user-scoped → any user reads/modifies any user's ebook.
- **[HIGH] Unauthenticated admin API surface:** `admin.ts` `isAdminRoute` misses `/api/landing-config` + `/api/v3/*` → anonymous POST `/api/v3/retention/trigger` enqueues jobs; anonymous POST `/api/landing-config` overwrites shared Redis key.
- **[HIGH] AI-config saves silently do nothing:** `ai-config.ts:17-77` validates `zodToJsonSchema(z.object({}))` → `additionalProperties:false` + ajv removeAdditional strips payloads; `:79-85` reset never clears.
- **[HIGH] Admin upload/deliver broken end-to-end:** `intercept.ts:113` requires `jobId+fileUrl`, `client.ts:1385` posts multipart FormData with only the file → always 400.
- **[HIGH] `/app/create` can never create a video:** `web/content.ts:78` hard-requires `niche`; only consumer `CreateVideo.tsx:17` sends `{prompt,style,duration}` → always 400 (web.test.ts:306 encodes the 400 as expected).
- **[MED]** `getDownloadUrl` embeds `?key=EBOOK_API_KEY` in redirect URL; analytics revenue filters `status='completed'` but code writes `"success"` → always empty; webhook.ts Midtrans signature over re-parsed body (JSON coercion "50000.00"→50000 → valid decimal notifications 401); `user-mgmt.ts:32` tier enum mismatch (free/pro/premium/enterprise vs real free/basic/lite/pro/agency); `GET /api/user/videos` leaks downloadUrl/userId/generationMetadata; JWT_SECRET/JWT_EMAIL_EXPIRES_IN absent from .env.example → startup fails; `web/chat.ts` POST `/api/chat/landing` zero consumers; Telegram HMAC verified but `auth_date` freshness never checked (replay).

### 3.4 Handlers / flows / workers / menus / middleware
- **[HIGH] Foto Bicara permanent dead-end:** `photo-upload.ts:251-257` dispatches `avatar_talk_photo_received` but `avatar-talk.ts:16` only handles `avatar_talk_start`; `handleAvatarTalkText` has no dispatcher.
- **[HIGH] Clone/Repurpose dead-ends:** `clone.ts:110-120` sets `CLONE_IMAGE_WAITING`/`REPURPOSE_VIDEO_URL`, nothing consumes; `callbacks/clone.ts:220,289` `repurposeData` only read, never set → repurpose_generate always fails; `video-uploader.ts:38` `handleDisassemble` never wired; storyboard `sba_*` never imported.
- **[HIGH] Menu dual-routing wipes state:** every `menu_*` token handled only by `bot.action` while `callbackHandler` runs in parallel → duplicate answerCbQuery rejected → catch resets state to 'DASHBOARD' + wipes stateData on every dashboard click; ~20 menu buttons no handler; 16 "Kembali" use `menu_main` vs ~80 `main_menu` (two tokens, two main menus).
- **[HIGH] Worker refund races:** `avatar-talk.worker.ts:50-76` attempts:2 without refund-lock → double failure = double refund, attempt-2 success = free video + refund; `retention.worker.ts` `createRetentionWorker()` no caller; `workers/index.ts:24` `startWorkers()` never called; `weekly-leaderboard.worker.ts` never invoked.
- **[HIGH] Flow state machines dead after refactor 6d27812:** `generate.input.ts` (`handleProductInput`/`handleMultiImageUpload`/`handleStoryboardEdit`/`handleTranscriptInput`) all dead → V3 product text falls into AI-chat fallback; user-edited scenes at `generate.ui.ts:444-484` never read by `executeGeneration` → Pro scene edits/uploaded images have zero effect.
- **[MED]** image-set res multiplier mismatch (confirm ×2/×4, charged 1.5); free-trial sets `welcomeBonusUsed` even when daily quota consumed; leaderboard reward increments `creditBalance` without audit; `generate.execution.ts:383,403` TypeError on 'custom' duration preset; payment `simulate_success_*` grants nothing.

### 3.5 i18n / MCP / views
- **[HIGH] MCP ebook IDOR** (see 3.1).
- **[MED]** MCP advertises niches absent from NICHES → "Invalid niche"; `create_video` enqueues chatId:0; `ai_chat` no credit check; `/faq` (`routes/web/pages.ts:39`) leaks `BOT_TOKEN.split(':')[0]`; 13 i18n keys missing from all 4 locales; **`id.json` 72 keys byte-identical to `en.json`**; tos.ejs malformed `<li)`.

### 3.6 Vendored tools
- **[HIGH] vidbee/krillinai/tiktok-downloader are gitlinks with NO `.gitmodules`** → fresh clone/CI leaves them EMPTY (tiktok-downloader additionally dirty: `main_terminal.py` gutted 2318→8 lines + 4 untracked files — orphan edits lost on re-clone).
- **[HIGH] VidBee unauthenticated token mint + RPC:** `local-api.ts:235-253` POST /automation/v1/handshake issues full-capability bearer token (CORS `*`); `apps/api/server.ts:118-121` no auth + reflect-any CORS on 0.0.0.0 → `/rpc/*` (listDirectories, openFile, deleteFile, uploadSettingsFile) unauthenticated.
- **[HIGH] VidBee excluded from runtime:** `tsconfig.json:25` excludes `src/tools/vidbee`; only consumer `services/download/cascade.py:18` uses `VIDBEE_URL` default localhost:3101 vs compose 3100; VIDBEE_URL absent from .env.example; no vidbee-api container → provider never exercisable.
- **[MED]** task-queue FSM: `handleFinish` 'error' branch force-fails + retry entry dangles → stuck 'failed'; `pause()` collapses to terminal 'cancelled', `resume()` gated on 'paused' → unreachable.

### 3.7 Python media-api — core & routers
- **[HIGH] Repurpose broken everywhere:** `video.py:830-851` `/video/repurpose` expects `RemetaAdRequest` but real callers (content-factory.service.ts:426, tiktok-automation.service.ts:358) POST `RepurposeRequest {sources:[]}` → 422 every call; legacy `/repurpose` (content.py) has the right model but nothing calls it.
- **[HIGH] `/video/remeta` always 500:** `video.py:807-825` parses `color_shift: Optional[str]`, callers send JSON bool → ValidationError → 422 → 500.
- **[HIGH] Bot TTS broken:** `audio.py:44-47`/`compat.py:30` `rate/pitch: Optional[float]` but bot sends "+0%"/"+0Hz" strings → 422; default request sends `rate=None` into `["--rate", None]` → subprocess TypeError → `/audio/speech` fails on EVERY default request.
- **[HIGH] Unauthenticated reverse-proxy to media-api:** `src/index.ts:302-313` `/api/py` proxy has no auth, and media-api exposes side-effect endpoints on the public host: `/download/video` (fetches arbitrary URL), `/upload/*`, `/publish-to-facebook`, `/cloak/post`, `/autopilot/run`; EBOOK_API_KEY guard optional + scoped only to `/text/ebook`.
- **[HIGH] Carousel 500:** `trends.py:46` calls `assembler.create(...)` but `CarouselGenerator` has no `.create` → AttributeError.
- **[MED]** `upload.py:30,52` no size/type limit, whole upload in memory → unbounded DoS on unauthenticated port; `if True:` always re-encodes H264; `startswith(str(base))` no os.sep → sibling named-prefix traversal; bare except → HTTPException(500, str(e)) leaks internal paths; blocking sync network/generation in async handlers stalls event loop; ab_test topic never persisted; research.py emits two "complete" events.

### 3.8 Python media-api — sub-services
- **[HIGH] movie_gen always fails at assembly:** `engine.py:159-163`/`video_gen.py:208-240` `render_scene_segment` returns tuple but engine appends un-unpacked → list of tuples; `_concat_with_crossfade` writes "duration 0.5" after every file line → every segment truncated to 0.5s; `_generate_narration` passes `output_path` positionally into `voice` → narration never lands in movie.
- **[HIGH] music/tts import failure silently degrades:** `music/__init__.py`+`tts/__init__.py` docstring-only (don't re-export) but consumers use flat import → ImportError swallowed → movie_gen BGM/narration always falls back to tone; `MusicGenerator().generate(mood=..., duration=30)` vs real `generate(prompt, duration_seconds=60)` → TypeError every time.
- **[HIGH] Engagement replies simulated:** `di.py:123`+`engagement/__init__.py:211-224` `/engagement/reply` instantiates AutoReplyEngine with NO cloak adapter → always simulated success.
- **[HIGH] Autopilot publish always fails:** `autopilot/orchestrator.py:206-212` calls `cloak_adapter.post(profile_id=None)` but `post()` has no auto-select → `resolved_id ""` → `/api/profiles//launch` malformed URL; `tiktok_publisher.py:149,168` reads `media_path` but carousel returns `slides`/`output_dir` → "Media file not found".
- **[HIGH] CloakAdapter reports failure as success + rejects TikTok:** `__init__.py:167` returns `{success:True, **post_result}` even when `_post_via_cdp` returned error dict; `:191-192` only `platform=="facebook"` accepted while primary consumers target TikTok → can never publish; `/cloak/batch-post` maps profile_ids into profile_name → "Profile not found".
- **[HIGH] Ebook manuscript fixes never reach export:** `manuscript_engine.py:176-181,259-261,318-386` assembles manuscript.md ONCE before retries; docx/epub export reads it → word-count/QA retries have no effect; `orchestrator.py:357-405` for/else+break flaw lets failing QA proceed to Export; StyleGuide generated but never passed to manuscript engine (banned phrases never applied); `_compute_price` always "$9.99".
- **[HIGH] PDF export always fails:** `export_orchestrator.py:120` `PdfConverter()` built with default `projects_dir="projects"` while API uses `data/ebook/projects` → `validate_project_path` always ValueError → `/download/pdf` 404s; `_generate_docx` stringifies dict → manifest paths are dict reprs; docx TOC always empty.
- **[HIGH] Remotion ads undeliverable:** `RenderAd.tsx:123` awaits `result.data.video_url` but remotion routes return `{status, data:{file_path,...}}` and no HTTP route serves `data/remotion/*.mp4`.
- **[MED]** download/cascade.py:107-154 non-TikTok URLs always "succeed" with 1×1 red placeholder PNG; snaptik `_SN_SALT` hardcoded; several `verify=False`; sync httpx.post (120s) from async handlers; movie_gen double narration; faceless TTS failure leaves `audio_path=''` then ffmpeg `-i ''` aborts whole video; comic panel_gen reads env `OMNIROUTE_URL` (wrong spelling) + default localhost:8000; remetadata `color_shift` bool/str inconsistent across two remeta endpoints; `/tmp` leaks; trends CLI calls nonexistent `scan_all()`.

### 3.9 admin-ui
- **[HIGH] Pages 404 + crashes:** `BookshelfPage.tsx:25` POST `/api/py/bookshelf/generate` → 404 (real `/text/book`); `ComicPage.tsx:22` `/api/py/comic/generate` → 404; `MoviePage.tsx:42,228` `/api/py/movie/generate` → 404 + sends `format` vs backend `style`; `AiConfigPage.tsx:120-202,460-467` Providers tab crashes after Test/Balance (`testResult[id].message.length` on undefined).
- **[HIGH] Tool pages can never submit:** `Cloak.tsx:39` posts `{profile_id,platform,caption}` but `CloakPostRequest.media_path` is REQUIRED → 422; `Engagement.tsx:36` omits required `profile_id` + sends non-backend `tone` → 422; `Storyboard.tsx:9,24` default `num_scenes=8` > backend `le=6` → 422.
- **[HIGH] Customer SPA broken flows:** `CreateVideo.tsx:17` missing required `niche` → always 400; `Billing.tsx:26-28` `topup_${amount}` package id never matches getPackagesAsync → always 400; `Referral.tsx:22-24` hardcodes `t.me/vilona_content_bot?start=${code}` but bot only honors `ref_` payloads and codes are `REF-*` → never tracked; `AuthContext.tsx:63` `isPremium = tier==="premium"` but tier is only free|basic|pro|agency → always false.
- **[MED]** Subscriptions reads `plan.price/creditsPerMonth` vs backend `monthlyPriceIdr/monthlyCredits`; getBilling hardcodes `credits:0`; MyVideos reads wrong fields; admin skin CSS depends on `var(--surface/--border/...)` defined only in `public/design-system.css` which is never loaded → cards/badges render transparent; landing `App.tsx:58` hardcodes `features:[]` → checkmark bullets vanish; `:61` replaces marketing tiers with credit packs.

### 3.10 Tests
- **[HIGH] Stale brand test — VERIFIED RED:** `help.test.ts:45` asserts "BERKAHKARYA AI" but help renders i18n `help.full_guide` = "1AI Content". **Verified live:** `npx jest tests/unit/commands/help.test.ts` → 1 failed/7; `jest tests/unit` → **1 failed/1267, 63 suites pass**. Contradicts root AGENTS.md "jest 1435 PASS" (brand revert 31f6d75 updated translations but not this test).
- **[HIGH] Entire bot e2e layer silently skips:** `tests/e2e/bot/conftest.py:28` `BOT_USERNAME="berkahkarya_saas_bot"` (bot is now `vilona_content_bot`) → Telethon `get_entity()` fails → whole suite skips; `:29` BASE_URL localhost:3000 (port empty; prod=3002, e2e=3111); `test_bot_e2e.py` empty BOT_TOKEN → KeyError hard-fail; `test_commands_registered` only prints, never asserts.
- **[MED]** `python-api-depth.spec.ts:128-131,155-158` hardcodes `{entries:[],count:0}` against live PROD :8767 + line 40 direct-hits prod health → data-dependent, isolation-breaking; several specs accept 404/500 as pass; rbac-security has `admin123` fallback; SESSION_PATH hardcoded `paijo`.

### 3.11 prisma
- **[HIGH] Massive schema↔migration drift:** ~45–50 models in schema but migrations create ~10 tables; **34 models have NO migration** (api_keys, social_accounts, payment_settings, user_avatars, pricing_config, saved_prompts, token_usage, user_streaks, user_badges, retention_logs, chat_events, video_clips, video_reworks, viral_scans, whitelabel_bots, all 9 yt_*, carousels, content_calendar, ab_tests, books, comics, movies, ebook_*, processed_videos). Live DB built via `prisma db push` (untracked) → `migrate deploy` on fresh DB yields broken schema while active code depends on those tables (ytChannel=29, whiteLabelBot=11, savedPrompt=11, apiKey=7 usages).
- **[HIGH] Migration ordering broken:** `20240319_enhanced_video_system/migration.sql` dated 2024-03-19 sorts BEFORE `20260315194752_init`, yet ALTERs `videos`/`generation_analytics` created only in init → fresh deploy fails "relation videos/users does not exist". Hand-written (old BERKAHKARYA header, custom idx_* names).
- **[HIGH] User columns unmigrated:** email, passwordHash, verification/reset columns, utm_*, fbc/fbp/ttclid, LTV columns, welcomeBonusUsed, dailyFree*, selectedNiche, isIntercepted (schema.prisma:85-142) → column-not-found at runtime.
- **[HIGH] camelCase raw SQL** — `subscription.service.ts:73,211` `telegramId` vs `telegram_id` (cross-confirmed with 3.1).
- **[MED]** `ecosystem_tracking.sql` hand-creates `facebook_pages` colliding with model `Fanpage @@map("facebook_pages")` (schema.prisma:1128), invisible to Prisma; 7 declared enums unused; tier vocabulary drift (free/basic/lite/pro/agency vs free/pro/premium/enterprise vs seed 'premium'); **Fanpage.accessToken stored plaintext** (schema.prisma:1117) vs YtChannel token "encrypted at app layer"; Carousel/ContentCalendar/ABTest defined in BOTH Prisma and Python SQLAlchemy (`services/db/models.py:140-220`); expiry path (`subscription.service.ts:196-202`) selects `{id, creditBalance}` but reads `subscriptionCredits` → subCredits=0 → expiry no-op.

### 3.12 Docker / CI / systemd / nginx
- **[HIGH] Docker image not buildable:** Dockerfile never runs `prisma generate` in any stage → tsc fails type resolution + image lacks query engine; image copies only node_modules/dist/config/package.json but runtime serves `admin-ui/dist`, `public/`, `admin-ui/dist/assets` (index.ts:322,332,342) → landing/admin/public 404; `npm ci` cannot resolve `@1ai/ai-pipeline`/`@1ai/platform-adapters` (`link:true → ../1ai/packages/...`) — no CI/Docker step provisions `../1ai` → all Node CI jobs + docker build hard-fail.
- **[HIGH]** CI docker-run smoke (`ci.yml:179-192`) omits required `ADMIN_PASSWORD`/`JWT_SECRET` → container crash-loops; only `sleep 10` + `docker logs`, no health/exit assertion → CI green while image provably broken.
- **[MED]** prometheus.yml scrapes `bot:3000/metrics` without credentials but `/metrics` is gated `adminCheck` (health.ts:186) → every scrape 401; grafana host port 3002 collides with prod PM2 :3002; `services/ebook/web/templates/nginx_snippet.txt` proxies dead 8765/8501; prod PM2 runs `tsx` while Docker runs `node dist/index.js` → dist artifact can silently drift.

### 3.13 scripts
- **[HIGH] Hardcoded real API key:** `scripts/test-ecosystem-integration.sh:21` `API_KEY="berkahkarya-ecosystem-2026-secure-key"` (secret in code).
- **[MED]** same script auths webhook with `X-Service-Key` but ecosystem.ts reads `x-api-key` → 401 (plus CONTENT_URL :3000 vs :3002); `restart-all.sh:32-48` targets PM2 apps `content-factory-api`/`vilonacontentbot` that don't exist (only `1ai-content`) + `|| true` swallows → restarts nothing; `verify_mlm_pricing.py` camelCase SQL vs snake_case schema + `run_sql` ignores errors → false-positive VERIFIED, hardcodes 15/5/2% vs real 10/5% in payment.yml; **`backup.sh:8,130` no `set -o pipefail` + `pg_dump 2>/dev/null | gzip` → failing dump becomes valid-but-empty gzip uploaded to S3 silently**.

### 3.14 config / .env.example
- **[HIGH] LIVE TikTok session cookies committed:** `config/tiktok_cookies.txt` is git-tracked (last commit a2b6c6c) on the public remote, NO .gitignore entry → real credential leak; needs removal from history + rotation.
- **[HIGH]** prometheus/grafana non-functional as shipped (401 scrapes; port 3002 clash).
- **[MED]** `.env.example` missing zod-REQUIRED `ADMIN_PASSWORD` and `JWT_SECRET` (env.ts:19-20 → startup `process.exit(1)`) plus ~116 other schema keys; ~30 documented vars DEAD (never read — TRACKING_URL, REMOTION_ENABLED, CIRCUIT_BREAKER_*, MAX_PROVIDER_RETRY, FALLBACK_CHAIN_MAX, BULLMQ_CONCURRENCY, VIDEO/IMAGE_QUEUE_NAME, POSTBRIDGE_*, ANTHROPIC_API_KEY, per-provider *_TIMEOUT/*_CB_*...); `1AI_PAYMENT_URL` default 3103 vs documented 3100; **entire config/*.yml set (ai/database/features/payment) is DEAD** — nothing reads it, yet config/README.md:16 claims runtime loading → payment.yml pricing/flags silently drift; payment.yml webhook paths `/webhooks/midtrans` vs code `/webhook/midtrans`; brand drift `berkahkarya` in .env.example lines 14/227/296.

### 3.15 docs / AGENTS / README / WORKFLOW / CHANGELOG
- **[HIGH] Docs describe code that doesn't exist:** `docs/01-ARCHITECTURE.md:23-26` claims `@fastify/cors`/`@fastify/rate-limit`/`asyncHandler` registered (none exist); `:222,229` + `docs/AGENTS.md:26` document framework as **grammY** — actual is **Telegraf**; `docs/04-FRONTEND.md:157-202` "EJS Views" spec references nonexistent files (only src/views/{admin/login, web/faq|tos|privacy, youtube/dashboard}.ejs exist).
- **[HIGH]** `docs/06-EXECUTION.md:320` post-deploy smoke greps "Vilona Content" — landing title is now "1AI Content — AI Content Factory" → documented smoke would fail.
- **[MED]** WORKFLOW.md:581-628 lists ~14 unregistered commands; 02-ROUTES.md lists nonexistent `/robots.txt`/`/sitemap.xml` + dead Python endpoints; 04-FRONTEND.md says React 18/RR6/base "/admin/" vs actual React 19.2.7/RR7/base "/"; 05-TESTING.md coverage numbers (85/78/65/72) fabricated vs actual thresholds 15/25; **CHANGELOG.md v3.0.0 aspirational copy** (RBAC/TOTP/hardware keys/KMS/DR/80% coverage/chaos tests — auth is password-only); admin-ui/AGENTS.md:55-63 still asserts deleted `Login.tsx`; systemd units shipped but disabled/stale (openclaw-saas-bot.service points at another repo; 1ai-content-bot.service hardcodes nvm v22.22.3 + :3000).

## 4. Cross-Cutting Themes

1. **Dead-end user pipelines** — UI buttons/callbacks/state machines silently dropping actions: menu dual-routing wiping state, unregistered `sba_*`/`cal_type_*`/`avatar_talk_*`/`carousel_*` handlers, dead `generate.input.ts`, `/broadcast` not sending, notification queue with no consumer.
2. **Fabricated success** — demo providers returning fake completed videos, canned analytics zeros, hardcoded system metrics, simulated engagement replies, cloak failure-as-success, fallback `success:true` on total failure, `verify_mlm_pricing`/`backup.sh` false positives.
3. **TS↔Python contract mismatch** — one model per endpoint not shared: `niche`, `rate/pitch` (str vs float), `RemetaAdRequest` vs `RepurposeRequest`, `media_path`, `num_scenes≤6`, intercept `jobId+fileUrl`, `format` vs `style`, `color_shift` bool vs str. Every one = permanent 422/500 on a live consumer path.
4. **Schema↔migration drift (prisma)** — 34 models unmigrated, broken ordering, camelCase raw SQL, dual Prisma/SQLAlchemy ownership, unused enums. `migrate deploy` on fresh DB fails outright; only `db push` masks it.
5. **Env/config surface chaos** — `.env.example` missing startup-required keys, ~30 dead vars, 3103 vs 3100, three OMNIROUTE_URL spellings, config/*.yml read by nothing.
6. **Secrets & security** — command injection (2×), committed TikTok cookies, hardcoded API key, plaintext FB tokens, `?key=` in redirect URLs, unauthenticated `/api/py` reverse-proxy + side-effect endpoints, ebook IDOR (HTTP + MCP), anonymous `/api/landing-config` + `/api/v3/*`, SSRF bypasses, per-IP rate limiter collapse.
7. **False-pass tests** — stale bot username skipping the whole Telethon suite, dead port 3000, brand-stale RED test (verified), empty-array hardcodes against prod, 404/500 accepted as pass. The e2e layer currently proves almost nothing.
8. **Orphaned/duplicate code & dead doc layer** — 20-file YT pipeline unwired, content-bot never imported, vidbee/krillinai gitlinks (empty on clone), orphaned `src/carousel/` Python, dead `money-printer-turbo`, ~30 dead env vars, docs describing grammY/EJS/fabricated coverage/aspirational changelog.
9. **Stale brand** — "berkahkarya"/"Vilona"/"openclaw-bot" across tests, scripts, systemd units, .env.example, docs, CHANGELOG.
10. **Billing/revenue broken end-to-end** — topup invalid package, subscription grant 42703, image-set pricing mismatch, refunds without dedup, credits not passed to execution, 1ai-payment port mismatch.
11. **Python hygiene** — sync blocking in async handlers, unbounded in-memory uploads, `/tmp` leaks, `verify=False`, bare `except` leaking paths in 500s.
12. **Admin UI depends on unstyled CSS** — design-system.css never loaded; several pages render transparent/unstyled.

## 5. Prioritized Remediation (top 20)

1. **CRITICAL — Fix command injection:** `content-pipeline.service.ts:81-82,121-122` + `audio-vo.service.ts:100-104` → execFile/child_process with args arrays; never shell-interpolate user input.
2. **CRITICAL — Remove `config/tiktok_cookies.txt` from git history** (filter-branch/BFG), add .gitignore, rotate the session.
3. **Prisma reconciliation:** `prisma migrate diff --from-schema-datasource --to-schema`; delete/fix 20240319 ordering bug; fix `subscription.service.ts:73,211` `telegramId`→`telegram_id` + `:196-202` expiry select.
4. **Make Docker/CI honest:** `prisma generate` + admin-ui build + copy `public/`/`admin-ui/dist` in Dockerfile; provision `../1ai` sibling; CI smoke sets ADMIN_PASSWORD/JWT_SECRET and asserts health; gate Trivy (exit-code 1).
5. **Fix TS↔Python contracts** at one canonical model per endpoint (niche, rate/pitch, repurpose, remeta color_shift, intercept jobId+fileUrl, num_scenes ≤6, media_path, MoviePage format→style) + cross-boundary contract test.
6. **Wire or delete dead state pipelines:** menu dual-routing, avatar-talk, clone/repurpose, storyboard sba_*, generate.input handlers, `/broadcast` + notificationQueue consumer, `startWorkers()`.
7. **Authenticate the Python surface:** auth preHandler on `/api/py`, scope side-effect endpoints, enforce EBOOK_API_KEY broadly; fix ebook IDOR (content-api + MCP); gate `/api/landing-config` + `/api/v3/*`.
8. **Fix `.env.example`:** add ADMIN_PASSWORD/JWT_SECRET + ~116 missing keys; delete ~30 dead vars; unify 1AI_PAYMENT_URL (3100) and OMNIROUTE_URL spelling; remove brand leftovers.
9. **Kill fake-success paths:** provider-registry demo provider, fallback success:true, systemStatus hardcoded metrics, engagement simulated posts, cloak error-as-success, carousel→autopilot media_path.
10. **Fix billing chain:** topup package IDs, CreateVideo niche, executeGeneration pricing multipliers, refund dedup, subscription expiry no-op, 1ai-payment port.
11. **Repair e2e:** conftest.py `BOT_USERNAME="vilona_content_bot"` + BASE_URL :3111; update help.test.ts brand; python-api-depth against isolated instance; replace print-only tests.
12. **Reconcile config/*.yml** (load or delete); fix `verify_mlm_pricing.py` (camelCase SQL, run_sql error checks, real rates) and `backup.sh` (`set -o pipefail`, pg_dump error propagation).
13. **Remove committed secret + fix scripts:** test-ecosystem-integration.sh API key + auth headers + :3002; restart-all.sh PM2 names; delete stale systemd units.
14. **Fix admin-ui:** AiConfigPage Providers crash, Bookshelf/Comic/Movie routes, Cloak/Engagement/Storyboard submit shapes, Billing/Referral/CreateVideo/AuthContext customer flows, load design-system.css.
15. **Docs overhaul:** 01-ARCHITECTURE grammY→Telegraf + plugin claims; 04-FRONTEND EJS section; 06-EXECUTION smoke grep; CHANGELOG aspirational copy; 02-ROUTES dead endpoints; WORKFLOW command table; admin-ui/AGENTS.md Login.tsx.
16. **Vendored tools:** add `.gitmodules` for vidbee/krillinai/tiktok-downloader (or vendor); decide vidbee runtime status; fix tsconfig exclusion vs cascade.py usage.
17. **Clean orphaned code:** move `src/carousel/` Python into services/, delete empty `money-printer-turbo`, remove content-bot barrel, drop vidbee exclusion if unused.
18. **Python security batch:** upload size/type limits, path traversal fix, remove `verify=False`, fix bare-except 500 leaks, non-blocking async handlers.
19. **Ebook export correctness:** PdfConverter projects_dir, docx stringification, StyleGuide wiring, retry/break logic, language passthrough.
20. **Ops nits:** Grafana port 3002→3003 + rotate admin password, `redis-cli -a` → REDISCLI_AUTH, remove `admin123` test fallbacks.

## 6. Verification Notes & Caveats

- **Live test runs (only ones performed):** agent-47 ran `npx jest tests/unit/commands/help.test.ts` → **1 failed / 7 passed**, and `jest tests/unit` → **1 failed / 1267 passed across 63 suites**. This is the single empirically-verified finding (stale brand in help.test.ts); it also disproves the root AGENTS.md "jest 1435 PASS" claim for the current tree.
- **Everything else is static-analysis output from the 56 subagents** (read-only scope; no files modified, nothing fixed, no builds/tests beyond the above were run). Severity totals approximate (±10%).
- **Cross-confirmed by independent agents:** command injection (a1/a22), carousel `.create` AttributeError (a29/a31), `niche` breaking `/app/create` (a13/a45), BookshelfPage 404 (a35/a42/a43), autopilot launch failure (a34/a37), prisma migration drift (a50/a55), subscription telegramId bug (a1/a55), TTS rate/pitch contract (a28/a30).
- **Not independently verified:** parent should spot-check highest-severity claims (RCE lines, prisma migration ordering, Dockerfile stages) against the code before acting — consistent across agents but not re-read by me.

**Bottom line:** a functional codebase with a serious gap between documented posture and runtime reality. The two damage classes worth attacking first: (1) RCE-grade shell interpolation + committed credentials — real, exploitable, present today; (2) pervasive TS↔Python and schema↔migration contract drift making large product areas (repurpose, TTS, admin upload, video creation, top-up, migrations) permanently broken rather than flaky.