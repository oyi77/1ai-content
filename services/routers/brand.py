"""Brand routes — brand-specific content generation (placeholder).

No /brand/* routes currently exist. Ready for future brand content endpoints.
"""
from fastapi import APIRouter

brand_router = APIRouter(prefix="", tags=["brand"])